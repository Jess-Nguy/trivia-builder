{
  "name": "trivia-builder",
  "version": "1.0.0",
  "description": "A localhost web app that imports trivia questions from a CSV and runs a host-controlled couch-party trivia night with manual scoring.",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "papaparse": "^5.4.1"
  }
}
